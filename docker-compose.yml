services:
  beneficial-belt:
    build: .
    container_name: beneficial-belt
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: 4321
    networks:
      - web
    # No ports published here - Caddy reaches this container over the shared network.

networks:
  web:
    external: true
