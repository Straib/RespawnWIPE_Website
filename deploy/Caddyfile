respawnwipe.de www.respawnwipe.de {
	reverse_proxy beneficial-belt:4321
}

# Frontend (Flutter web) is hosted separately on the Strato webserver, not this VPS.
api.rpo-estimation-tool.com {
	reverse_proxy estimation-tool-server:8080
}

pscp.exe deploy\docker-compose.yml deploy\Caddyfile root@87.106.168.192:/opt/infra/deploy/